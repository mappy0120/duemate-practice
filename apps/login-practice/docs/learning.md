# 作りたいもの

メールアドレスとパスワードを入力し、ログインボタン押下時に値を送信する。

# 何が起きるのか

- メールアドレス入力
- serverに送る
- serverが値を保持する
- ログインボタンを押す
- serverへemailとpasswordを送信する
- server側でデーターベースに存在するユーザーと比較する
- 一致すればログイン成功
- 一致しなければエラー表示

# 必要そうな概念

- input
